-- Active: 1768913406748@@127.0.0.1@5432@fusion
python manage.py runserver

python manage.py makemigrations # toda vez que criamos modelo executamos esse comando
python manage.py migrate # realiza a migração cria tabelas


from django.contrib.auth.models import User
#usuario e senha comando
python manage.py createsuperuser
is_staff = true  e is_superuser = true se  for cretesupperuser
is_staff = true  e is_superuser = false se for membro da equipe  User.objects.create_user(username = 'teste', password = '123456', email = 'teste@gmail.com')
is_staff = false  e is_superuser=false se não for membro da equipe User.objects.create_user(username = 'teste', password = '123456', email = 'teste@gmail.com')
User.objects.create_user(
    username='teste1', 
    password='1234567', 
    email='teste1@gmail.com', 
    is_staff=True
)
python manage.py collectstatic #usado quando DEBUG=False


pip freeze > requirements.txt # manter atualizado dependencias do projeto
pip install -r requirements.txt
pip install  --requirement requirements.txt

para trabalhar com arquivos estaticos em produção pip install whitenoise   gunicorn é um servidor 

depois do login 
heroku create django1-pi --buildpack heroku/python
git push heroku master #para publicar app

. Criar o Projeto django

O comando principal para gerar a estrutura base do site:

django-admin startproject nome_do_projeto .

criando app
as duas linhas abaixo cria app, só use uma linha
#python manage.py startapp nome_da_app
django-admin startapp core

5. Executar as Migrações Iniciais

Cria as tabelas de base de dados necessárias para o sistema de administração e utilizadores:

python manage.py migrate

iniciar servidor 
python manage.py runserver
 
usar o shell
python manage.py shell

Criar um Superutilizador (Admin)

Para aceder à interface de administração (/admin)

python manage.py createsuperuser

instalando para projeto 2
pip install django whitenoise gunicorn django-bootstrap4 PyMysql django-stdimage
django-admin startproject django2 .
criando app core
#python manage.py startapp core vou usar linha debaixo as duas cria app
django-admin startapp core


depois de criar o model rodar
python manage.py makemigrations e depois a linha abaixo
python manage.py migrate cria todas as tabelas